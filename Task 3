
#include <iostream>
#include <string>
using namespace std;

int main()
{
    string s;
    int l;
    cin >> s;
    l = s.length();

    for (int i = 0; i < l; i++)
    {
        if (s[i] == '0')
        {
            s[i] = '1';
        }
    }
    cout << s;
    system("pause");
}
