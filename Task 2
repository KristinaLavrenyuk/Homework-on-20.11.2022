
#include <iostream>
#include <string>
using namespace std;

int main()
{
    int n, maxL = 0, l;
    string s, maxS;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> s;
        l = s.length();
        if (maxL < l)
        {
            maxL = l;
            maxS = s;
        }
    }
    cout << maxS;
    system("pause");
}
